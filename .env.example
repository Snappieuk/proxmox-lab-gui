# Flask Configuration
SECRET_KEY=change-me-to-random-string-in-production
FLASK_ENV=production

# Optional: Performance tuning
# VM_CACHE_TTL=300

# Optional: Admin configuration (legacy - use database for user management)
# ADMIN_USERS=root@pam
# ADMIN_GROUP=adminers

# Note: Proxmox cluster configuration is now managed via the web UI at /admin/settings
# No need to configure PVE_HOST, PVE_ADMIN_USER, or PVE_ADMIN_PASS here
