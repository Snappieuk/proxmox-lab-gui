name: Auto Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest changes and restart Flask app
        run: bash ~/rdp-gen/deploy.sh
