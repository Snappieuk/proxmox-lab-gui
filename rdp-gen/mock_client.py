"""
mock_client.py removed — mock client implementation is not maintained.
If you need a local dev stub, create your own minimal wrapper that implements
get_nodes(), list_qemu(node), and status_qemu(node, vmid) (see cache.py).
"""
